server:
 port: 8889

spring:
 application:
  name: config-server
 cloud:
  config:
   server:
    git:
     uri: https://github.com/wangke839/springcloud-demo.git
     search-paths:
      - configfiles
   label: test